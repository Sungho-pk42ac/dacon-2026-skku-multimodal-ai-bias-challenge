# 데이터/가중치/출력 (용량 큼 — 버전관리 제외)
data/images/
data/*.csv
data/*.jsonl
outputs/
*.safetensors
*.bin

# 제출물은 보관(작게) — 필요시 주석 해제로 제외
# submissions/

# 환경/캐시
__pycache__/
*.pyc
.venv/
.env
.ipynb_checkpoints/
wandb/
runs/

# API 키 — 절대 커밋 금지
*.key
secrets.*
.env

# 하네스/에디터 내부 상태 (저장소에 불필요)
.omc/
.claude/

# 강의 자료(저작권) + 출력에 토큰 잔존 → 로컬 참고용만
notebooks/

# pod 접속 헬퍼(휘발성)
infra/pod.sh
infra/pod_push.sh

# 2차 제출물 — 개인정보/대용량 (공개 레포에 커밋 금지)
submission/참가자격증빙/
제출메일_pk42ac.txt
데이콘_2차평가산출물.zip
